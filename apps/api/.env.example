# Local development only. Do not use these credentials in production.
DATABASE_URL="postgresql://opsboard:opsboard@localhost:5435/opsboard?schema=public"

# Authentication values will be defined when the auth module is implemented.
JWT_SECRET="replace-this-with-a-long-random-secret"
